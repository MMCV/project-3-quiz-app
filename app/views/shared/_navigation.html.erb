<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if logged_in? %>
        <% if current_user.type == 'Student' %>
        <a class="navbar-brand" href= "/students/<%= current_user.id%>" >Quiz!</a>
        <% elsif current_user.type == "Instructor" %>
          <a class="navbar-brand" href= "/instructors/<%= current_user.id%>" >Quiz!</a>
        <% end %>
      <% else %>
      <a class="navbar-brand" href="/">Quiz!</a>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li> <button type="button" onclick="location.href='/logout';" class="btn btn-link navbar-btn navbar-right">Log out</button></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if current_user.type == "Instructor" %>
                <li><%= link_to "Create a Quiz", new_quiz_path %> </li>
                <li><%= link_to "See all Quizzes", quizzes_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Create a Cohort", new_cohort_path %></li>
                <li><%= link_to "Join a Cohort", instructorsignup_path %></li>
              <% else %>
                <li><%= link_to "Join a Cohort", studentsignup_path %></li>
              <% end %>
          <% else %>
          <button type="button" onclick="location.href='/login';" id="sign-in" class="btn btn-default navbar-btn navbar-right sign-in-button">Sign in</button>
          <button type="button" onclick="location.href='/users/new';" class="btn btn-default navbar-btn navbar-right">Sign up</button>
          <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
