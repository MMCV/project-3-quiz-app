<%if logged_in? %>
  <%= react_component 'ShowInstructor', instructor: @instructor, cohorts: @instructor_cohort, students: @student_list %>
  <button><a href="/quizzes/new">New Quiz</a></button>
  <button><a href='/quizzes'>All Quizzes</a></button>
<%else%>
  <% redirect_to "/" %>

<%end%>
